name: Deploy

on:
  push:
    branches:
      - "release"
    tags-ignore:
      - "**"

env:
  CONTAINER_REGISTRY_URL: ghcr.io
  IMAGE_NAME: emadb/lorx

jobs:
  setup:
    runs-on: [self-hosted]
    outputs:
      version: ${{ steps.configure.outputs.version }}
    steps:
      - id: configure
        name: Configure environment
        run: |
          VERSION=`date '+%Y.%m%d.%H%M'`
          echo "version=$VERSION" >> $GITHUB_OUTPUT

  package:
    runs-on: [self-hosted]
    needs: setup
    env:
      VERSION: ${{ needs.setup.outputs.version }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          platforms: linux/arm64
      - name: Build and push core image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: Dockerfile
          tags: "${{ env.CONTAINER_REGISTRY_URL }}/${{ env.IMAGE_NAME }}:${{ env.VERSION }}"
          push: true

  tag-and-release:
    runs-on: [self-hosted]
    needs: setup
    env:
      VERSION: ${{ needs.setup.outputs.version }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Create tag and release
        run: gh release create ${{ env.VERSION }} --title ${{ env.VERSION }} --target ${{ github.REF_NAME }} docker-compose-server.yml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  deploy:
    runs-on: [self-hosted]
    needs: [setup, package, tag-and-release]
    environment: prod
    env:
      VERSION: ${{ needs.setup.outputs.version }}
    steps:
      - name: Download artifacts
        run: gh release download ${{ env.VERSION }} -R ${{ github.REPOSITORY }} --clobber
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Deploy
        run: |
          docker compose -f docker-compose-prod.yml pull app
          docker compose -f docker-compose-prod.yml up -d
          sleep 3
          docker compose -f docker-compose-prod.yml exec app /app/bin/lorx eval "Lorx.Release.migrate()"
        env:
          VERSION: ${{ env.VERSION }}
          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
          SECRET_KEY_BASE: ${{ secrets.SECRET_KEY_BASE }}
          ENV_NAME: ${{ vars.ENV_NAME }}
          DEVICE_POLLING_INTERVAL: ${{ vars.DEVICE_POLLING_INTERVAL }}
          SAVING_INTERVAL: ${{ vars.SAVING_INTERVAL }}
          POWER_METER_IP: ${{ vars.POWER_METER_IP }}
